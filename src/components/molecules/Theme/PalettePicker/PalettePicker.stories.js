import { SB_LABELS } from '@constants';
import PalettePicker from './PalettePicker.react';

export default {
  title: `${SB_LABELS.MOLECULES}Theme/PalettePicker`,
};

export const Default = PalettePicker;
