import { SB_LABELS } from '@constants';
import { IconPolygon } from '@icons';
import React from 'react';

export default {
  title: `${SB_LABELS.ICONS}/Polygon`,
};

export const Default = () => <IconPolygon />;
