import { SB_LABELS } from '@constants';
import React from 'react';
import Icon from './Twitter.svg.react';

export default {
  title: `${SB_LABELS.ICONS}Twitter`,
};

export const SVG = () => <Icon />;
