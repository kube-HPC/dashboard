import { SB_LABELS } from '@constants';
import React from 'react';
import Icon from './Stackoverflow.svg.react';

export default {
  title: `${SB_LABELS.ICONS}Stackoverflow`,
};

export const SVG = () => <Icon />;
