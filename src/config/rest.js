const name = `rest`;

const rest = {
  STATE: {
    name,
    delete: `${name}/delete`,
    fileDownload: `${name}/fileDownload`,
    get: `${name}/get`,
    post: `${name}/post`,
    put: `${name}/put`,
  },
};

export default rest;
