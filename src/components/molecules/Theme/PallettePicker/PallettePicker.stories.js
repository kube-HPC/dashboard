import { SB_LABELS } from '@constants';
import PallettePicker from './PallettePicker.react';

export default {
  title: `${SB_LABELS.MOLECULES}Theme/PallettePicker`,
};

export const Default = PallettePicker;
