const actionNames = {
  addPipeline: `Add Pipeline`,
  addAlgorithm: `Add Algorithm`,
  runRawPipeline: `Run Raw Pipeline`,
  addDebug: `Add Debug`,
};

export const actionValues = Object.values(actionNames);

export default actionNames;
