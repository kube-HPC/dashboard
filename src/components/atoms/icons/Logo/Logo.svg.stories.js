import { SB_LABELS } from '@constants';
import { IconLogo } from '@icons';
import React from 'react';

export default {
  title: `${SB_LABELS.ICONS}/Logo`,
};

export const Default = () => <IconLogo />;
