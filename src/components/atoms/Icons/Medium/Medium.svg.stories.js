import { SB_LABELS } from '@constants';
import React from 'react';
import Icon from './Medium.svg.react';

export default {
  title: `${SB_LABELS.ICONS}Medium`,
};

export const SVG = () => <Icon />;
