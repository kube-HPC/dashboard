import { SB_LABELS } from '@constants';
import React from 'react';
import Icon from './Github.svg.react';

export default {
  title: `${SB_LABELS.ICONS}Github`,
};

export const SVG = () => <Icon />;
