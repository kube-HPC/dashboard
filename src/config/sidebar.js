const mapper = {
  jobs: `Jobs`,
  pipelines: `Pipelines`,
  algorithms: `Algorithms`,
  workers: `Workers`,
  drivers: `Drivers`,
  debug: `Debug`,
  analytics: `Analytics`,
};

const values = Object.values(mapper);

const sidebar = {
  mapper,
  values,
};

export default sidebar;
