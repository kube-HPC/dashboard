import { SB_LABELS } from '@constants';
import React from 'react';
import Intro from './Intro.react';

export default {
  title: `${SB_LABELS.ATOMS}Intro`,
};

export const Default = () => <Intro />;
